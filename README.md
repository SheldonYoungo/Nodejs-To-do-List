# Nodejs To do list

This was a simple project made to practice Nodejs.

## Usage

Run the app.js file with node into the terminal

```bash
node app.js
```
Then open your browser and search for localhost:3000
